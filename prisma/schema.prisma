generator client {
  provider = "prisma-client"
  output   = "../app/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Student {
  id        Int      @id @default(autoincrement())
  firstName String
  lastName  String
  year      Int
  referrer  String
  notes     String   @default("")
  atRisk    Boolean  @default(false)
  weOnly    Boolean  @default(false)
  createdAt DateTime @default(now())
  tasks     Task[]
}

model Task {
  id        Int      @id @default(autoincrement())
  studentId Int
  student   Student  @relation(fields: [studentId], references: [id], onDelete: Cascade)
  title     String
  category  String
  status    String   @default("NOT_STARTED")
  date      String
  notes     String   @default("")
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  taskNotes       TaskNote[]
  taskAttachments TaskAttachment[]

  // Work Experience extra fields
  weCompany            String  @default("")
  weContactName        String  @default("")
  weContactPhone       String  @default("")
  weContactEmail       String  @default("")
  weStartDate          String  @default("")
  weEndDate            String  @default("")
  weSPR                Boolean @default(false)
  weMyWorkExperience   Boolean @default(false)
  weMedicalDocs        Boolean @default(false)
  weWorkplaceVisited   Boolean @default(false)
  weSafetyGuideParent  Boolean @default(false)
  weSafetyGuideEmployer Boolean @default(false)
  weSortOrder           Int?
}

model WEEmployer {
  id           Int      @id @default(autoincrement())
  company      String
  contactName  String   @default("")
  contactPhone String   @default("")
  contactEmail String   @default("")
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
}

model TaskNote {
  id        Int      @id @default(autoincrement())
  taskId    Int
  task      Task     @relation(fields: [taskId], references: [id], onDelete: Cascade)
  content   String
  createdAt DateTime @default(now())
}

model TaskAttachment {
  id        Int      @id @default(autoincrement())
  taskId    Int
  task      Task     @relation(fields: [taskId], references: [id], onDelete: Cascade)
  name      String
  url       String
  size      Int
  mimeType  String
  createdAt DateTime @default(now())
}
